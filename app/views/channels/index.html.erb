  <ul class="menu w-56 border-[1px] rounded-lg">
    <% @channels.each do |channel| %>
      <li><%= link_to channel.name, channels_show_url(id: channel.id), data: {turbo_advance: true, turbo_frame: "chat"} %></li>
    <% end %>
  </ul>
<%= link_to "Create new channel", channels_new_path, class: "btn btn-outline" %>
<%#= turbo_stream_from "chatroom" %>
  <%= render "chatroom", channel: nil %>
